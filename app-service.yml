apiVersion: v1
kind: Service
metadata:
  name: springbootapp-service
spec:
  type: NodePort
  ports:
    - name: "http"
      port: 49161
      targetPort: 8080
      nodePort: 30001
    - protocol: TCP
      port: 8126
      targetPort: 8126
  selector:
    io.kompose.service: springbootapp
